

<%= render @expense %>

<div>
  <%= link_to "Edit this expense", edit_expense_path(@expense) %> |
  <%= link_to "Back to expenses", expenses_path %> |
  <%= link_to "Comments", expense_comments_path(@expense)%>

  <%= button_to "Destroy this expense", @expense, method: :delete %>
</div>
